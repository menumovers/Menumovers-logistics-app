I need your help getting the environment caught up. Here's what changed and why, so you have the full picture rather than just a checklist.

The one thing that needs your attention: a new database column. orders gained a nullable timestamp column, en_route_to_customer_at (Drizzle schema in lib/db/src/schema/orders.ts). It's set when a rider's status moves to en_route_to_customer, and it powers a live "N min underway" counter on the rider's order screen. It's purely additive — nullable, no default, no backfill needed, doesn't touch or rename anything existing — so pushing the schema should be a clean no-prompt change. That said, please still review whatever diff/prompt the push shows you before applying it, the way you normally would; I'd rather you catch something I've missed than trust my "should be safe" at face value. Once it's applied, worth confirming the drift checks come back clean.

Until this column exists in the live database, any order that transitions to en_route_to_customer will fail (the API tries to write to a column that isn't there yet) — so this is the blocking piece, not an optional cleanup.

One build-process reminder, since it's bitten this project before: the API contract (lib/api-spec/openapi.yaml) picked up several new endpoints and schemas this round — a rider-reassignment endpoint, an order-history endpoint, and two trip-membership endpoints. The generated client source for those is already committed to the repo, but the compiled dist/ output for the generated packages (lib/api-zod, lib/api-client-react) is not — it's gitignored and gets rebuilt fresh. If your normal install/build flow doesn't already rebuild those composite packages, make sure it does, otherwise the API server and frontend will be working against stale compiled types even though the source is current.

No new environment variables, secrets, or external services are involved in any of this — everything above is internal to the existing database and build pipeline.

Once you've got things deployed, it'd be worth exercising a few of the new flows against the live environment to make sure they behave the way they're supposed to — a rider order actually reaching en_route_to_customer and showing the live counter, a coordinator reassigning or unassigning a rider on an in-flight order, adding and then removing an order from an existing trip (and confirming the trip completes itself once its last order finishes), and paging through the new order-history screen to see that an order gets its customer details hidden once it's more than 24 hours past delivery. Use your judgment on how deep to go — you know this environment's quirks better than I do at this point.

If you want the fuller reasoning behind any of this — why unassignment has no "hasn't left yet" cutoff, why trip membership is now editable after creation, why the 24-hour redaction window works the way it does — it's written up in docs/workflow-decisions.md under D18, D19, and D20, and the day-of changelog entries are in docs/changelog.md.